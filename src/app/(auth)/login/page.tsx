import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Login',
};

function Login() {
  return <h1 className="flex justify-center pt-64 text-5xl">LOGIN</h1>;
}

export default Login;
