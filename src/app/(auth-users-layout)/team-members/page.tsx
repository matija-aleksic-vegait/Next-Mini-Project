import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Team Members",
};

function TeamMembers() {
  return <h1 className="flex justify-center text-5xl"> TEAM MEMBERS</h1>;
}

export default TeamMembers;
